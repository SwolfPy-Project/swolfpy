# Check our guides at https://confluence.atlassian.com/x/x4UWN for more examples.
# Only use spaces to indent your .yml configuration.
# You can specify a custom docker image from Docker Hub as your build environment.

image: sardarmehni/swolfy:latest

pipelines:
  default:
    - step:
        script:
          - conda activate swolfpy 
          - pip3 install https://bitbucket.org/swolfpy/swolfpy-inputdata/get/master.zip
          - pip3 install https://bitbucket.org/swolfpy/swolfpy-processmodels/get/master.zip
          - ls -R /opt/atlassian/pipelines/agent/build/
          - ls -R /opt/atlassian/pipelines/agent/build/tests
          - pytest



